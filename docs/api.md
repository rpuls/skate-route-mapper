# API Contract

This document defines the backend API that the React Native app should integrate with.

The goal is to keep the first version very simple:

1. Start a ride
2. Upload ride samples in batches
3. Finish the ride

That is the entire write flow the mobile app needs.

## Base URL

Local development:

```text
http://localhost:3001
```

Production:

```text
Set by deployment environment
```

## General Rules

- All request and response bodies are JSON.
- Timestamps are Unix time in milliseconds.
- `rideId` is generated by the client.
- The mobile app should create one `rideId` per ride session.
- Sample uploads must be sent after the ride has been started.
- Samples cannot be uploaded after the ride has been finished.
- The backend currently has no auth yet.

## Recommended Mobile Flow

For each ride session, the app should do this:

1. Generate a `rideId`.
2. Call `POST /v1/rides/start`.
3. Start collecting samples locally.
4. Upload samples in batches while recording.
5. Call `POST /v1/rides/:rideId/finish` when the user stops recording.

Recommended batching:

- send batches instead of one request per sample
- keep a local queue in case the network is temporarily unavailable
- a good first batch size is `25` to `100` samples

## Data Types

### VehicleType

```ts
"skates" | "skateboard" | "longboard"
```

### SensorSource

```ts
"phone" | "external"
```

### MeasurementSample

```ts
type MeasurementSample = {
  timestamp: number;
  ax: number;
  ay: number;
  az: number;
  gx: number;
  gy: number;
  gz: number;
  vibrationMagnitude: number;
  latitude: number | null;
  longitude: number | null;
  speed: number | null;
};
```

Notes:

- `vibrationMagnitude` must already be calculated by the mobile app.
- `latitude`, `longitude`, and `speed` may be `null`.
- `speed` should be meters per second if provided by the mobile app.

## Endpoints

### `GET /health`

Simple health check.

Response:

```json
{
  "ok": true,
  "service": "skate-route-mapper-api"
}
```

The RN app does not need this endpoint for normal ride uploads.

### `POST /v1/rides/start`

Create or register a ride session before sending samples.

Request body:

```json
{
  "rideId": "0d4c61cf-1d7a-4ca8-9e56-fd4185dd0df8",
  "startedAt": 1760000000000,
  "vehicleType": "skates",
  "sensorSource": "phone",
  "clientId": "device-abc-123",
  "appVersion": "1.0.0",
  "deviceModel": "iPhone 15"
}
```

Required fields:

- `rideId`
- `startedAt`
- `vehicleType`
- `sensorSource`

Optional fields:

- `clientId`
- `appVersion`
- `deviceModel`

Success response:

Status:

```text
201 Created
```

Body:

```json
{
  "ok": true,
  "rideId": "0d4c61cf-1d7a-4ca8-9e56-fd4185dd0df8"
}
```

Behavior notes:

- If the same `rideId` is sent again, the server currently ignores the duplicate insert and still responds successfully.
- The RN app should still treat `rideId` as unique per ride.

### `POST /v1/rides/:rideId/samples`

Upload a batch of measurement samples for an existing ride.

Path params:

- `rideId`: the same client-generated ride ID used in the start call

Request body:

```json
{
  "samples": [
    {
      "timestamp": 1760000001000,
      "ax": 0.1,
      "ay": -0.2,
      "az": 0.97,
      "gx": 0.01,
      "gy": 0.02,
      "gz": 0.03,
      "vibrationMagnitude": 0.995,
      "latitude": 52.3676,
      "longitude": 4.9041,
      "speed": 3.5
    },
    {
      "timestamp": 1760000001200,
      "ax": 0.11,
      "ay": -0.22,
      "az": 0.95,
      "gx": 0.02,
      "gy": 0.01,
      "gz": 0.04,
      "vibrationMagnitude": 0.981,
      "latitude": 52.3677,
      "longitude": 4.9042,
      "speed": 3.4
    }
  ]
}
```

Rules:

- `samples` must contain at least `1` sample
- `samples` may contain at most `1000` samples per request
- samples should belong to the ride identified by `rideId`

Success response:

Status:

```text
202 Accepted
```

Body:

```json
{
  "ok": true,
  "rideId": "0d4c61cf-1d7a-4ca8-9e56-fd4185dd0df8",
  "accepted": 2
}
```

Behavior notes:

- The API does not currently deduplicate samples.
- If the same batch is uploaded twice, it will be stored twice.
- The mobile app should therefore avoid retrying blindly without tracking what was already sent.

### `POST /v1/rides/:rideId/finish`

Mark a ride as finished.

Path params:

- `rideId`: the same client-generated ride ID used in the start call

Request body:

```json
{
  "endedAt": 1760000900000
}
```

Success response:

Status:

```text
200 OK
```

Body:

```json
{
  "ok": true,
  "rideId": "0d4c61cf-1d7a-4ca8-9e56-fd4185dd0df8"
}
```

Important:

- After a ride is finished, later sample uploads for that ride will be rejected.

### `GET /v1/rides`

List rides from the backend.

This is mainly for admin tools, not required for the mobile write flow.

Query params:

- `limit`: optional, max `100`, default `25`

Example:

```text
GET /v1/rides?limit=20
```

### `GET /v1/rides/:rideId`

Fetch one ride and all of its stored samples.

This is mainly for admin tools, debugging, or later sync features.

## Error Responses

### `400 Bad Request`

Used when the request body is invalid.

Example:

```json
{
  "ok": false,
  "message": "Invalid request body",
  "issues": [
    {
      "path": ["samples", 0, "latitude"],
      "message": "Too small: expected number to be >=-90"
    }
  ]
}
```

### `404 Not Found`

Used when the ride does not exist.

Example:

```json
{
  "ok": false,
  "message": "Ride not found"
}
```

### `409 Conflict`

Used when trying to upload samples to a ride that has already been finished.

Example:

```json
{
  "ok": false,
  "message": "Ride already finished"
}
```

### `500 Internal Server Error`

Used for unexpected backend failures.

Example:

```json
{
  "ok": false,
  "message": "Internal server error"
}
```

## What The React Native App Should Implement First

The RN developer only needs to build this first:

1. Generate `rideId` on ride start
2. Call `POST /v1/rides/start`
3. Buffer samples locally while recording
4. Send sample batches to `POST /v1/rides/:rideId/samples`
5. Retry carefully if a batch upload fails
6. Call `POST /v1/rides/:rideId/finish` when recording ends

Good first simplifications:

- no auth yet
- no pause/resume API yet
- no delete ride API yet
- no server-side sample deduplication yet
- no offline sync conflict logic yet

## Current Contract Summary

The write contract is intentionally small:

- one start endpoint
- one sample batch endpoint
- one finish endpoint

That is the contract the RN app should target unless we agree to change it.
